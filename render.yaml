services:
  - type: web
    name: fridgechef
    runtime: docker
    plan: free
    region: singapore
    healthCheckPath: /api/health
    envVars:
      # ชี้ไป Supabase Postgres (ADR 0005) — กรอกค่าใน Render dashboard ตอน Apply blueprint
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
